import { SongFile } from '../../utils/types';

export const mockSongFileResponse: SongFile[] = [
  {
    file_url: 'http://mock_musicfy_api.com/song.mp3',
    type: "music"
  }
];

export const mockSongNameResponse: string = 'Mock Song Name';